# don't break on SIGWINCH or SIGCHLD
process handle -p true -n false -s false SIGWINCH
process handle -p true -n false -s false SIGCHLD
type summary add pty_host --summary-string "[${var.rect.client.h}x${var.rect.client.w}] ${var.title%s}"

command script import ./debugging/display.py 
